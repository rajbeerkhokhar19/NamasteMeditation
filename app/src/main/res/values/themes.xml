<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NamasteMeditation" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NamasteMeditation" parent="Base.Theme.NamasteMeditation" />

    <style name="Menu.TextAppearance" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:fontFamily">@font/raleway_regular</item>
        <item name="android:textColor">@drawable/selector_menu_item</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="customRatingBar" parent="android:style/Widget.Material.RatingBar.Small">
        <item name="colorControlNormal">@color/grey_ababab</item>
        <item name="colorControlActivated">@color/orangeLight</item>
    </style>

    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/bg_dialog_rounded</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/grey_ababab</item>
        <item name="colorControlActivated">@color/orangeLight</item>
    </style>
</resources>